#include <stdio.h>

/**
 * main - Entry point
 * prinf - Prints the output
 * Return: 0 (Success)
 */
int main(void)
{
	char num;

	while (num <= 9)
	{
		printf('%c' num);
	}
	purchar('\n');
	return (0);
}
