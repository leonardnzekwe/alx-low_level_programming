#include <stdio.h>

/**
 * main - Entry point
 * prinf - Prints the output
 * Return: 0 (Success)
 */
int main(void)
{
	int num;

	while (num <= 9)
	{
		printf("%d", num);
	}
	purchar('\n');
	return (0);
}
