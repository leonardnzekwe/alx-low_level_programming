#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	/* 
	 * printf  - prints the formatted string
	 */
	printf("%s\n" "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
